# Typst playtest handoff — 2026-09-22

Branch: `codex/typst-user-playtest`. Code fix: `a75075e1`; subsequent changes only document the handoff. Separate draft PR; no merge.

## Observed defect and bounded fix

Reopening a saved review with one note showed “1 notes” in the status and library. Doc Viewer now shares consistent singular/plural wording across save, reopen and library messages. No saved-review format or behavior changes.

- Existing Doc Viewer suite: 83/83 pass, no skips. Typst and Doc Viewer `harness dsh check`, JavaScript syntax and `git diff --check` pass.
- Typst's 28 baseline helper/setup checks were covered across the main run and an explicit pinned-compiler rerun. Pinned Typst is 0.15.1; pdf.js is 6.3.289.
- The corrected Doc Viewer package is installed from the official review branch at `a75075e1`; setup/doctor pass. Visual recheck of the corrected copy remains pending after reloading the native viewer.

## Completed browser review journey

In a new scratch workspace, compiled a three-page fictional FieldNotes brief with real Typst. In Chrome, held the draft, selected a page-2 conflict-policy sentence, added an anchored Change note and kept **Field Notes · conflict policy review**. Read the saved packet and revised the original Typst source through the compiler. Compare latest correctly said the old quote was absent. Reviewed draft restored the original PDF and highlight. Reloaded, reopened the saved review and verified its text/anchor again.

This was actual browser use of real PDF artifacts; the source revision in this scratch journey was performed by the tester, not the embedded Claude agent.

Scratch workspace: `/private/tmp/openharness-typst-user-playtest/.scratch/playtest/workspace`.
Saved packet: `.harness/doc-reviews/6acc28ff-92ea-491b-8273-aebf30206818/`.
Original PDF SHA-256: `9a3ed406183400e6ce6fbccaa268e00e5de9ae1e0c865965c214f4ec5ff1c400`.

## Actual Harness prompt completed; native review/revision pending

Opened Store → Typst → New Harness → Start Harness and submitted an original prompt to embedded Claude: create a polished three-page fictional offline field-notes engineering brief comparing local-only, sync-only and offline-first designs, with a risk table and three-week plan.

Continue in tab **Field-notes app engineering brief**, project `~/harnesses/typst-2026-09-22-13-07`. Claude produced **Fieldbook — Sync Architecture Decision Brief**, three pages, verdict ready with no findings. Its original PDF is copied to `.scratch/playtest/native-typst-original.pdf`; SHA-256 `a56bd219a17c9ab1aa4b67693bd5be71f414d9ba3c63a146f699d134d3b3385d`.

## Resume

1. Reload the native viewer to load the installed copy fix, then visually inspect all three actual agent-authored pages.
2. Hold the draft and anchor a review on the conflict-policy inconsistency: “Last-writer-wins” versus the adjacent promise to retain both conflicting values. Keep one Change note; verify “1 note” in saved/reopened labels.
3. Ask embedded Claude to apply the saved review, preserve the packet and recompile three pages. Leave preview/desktop/browser interaction to the tester; ask the agent to author files and inspect artifacts.
4. Compare the new PDF, switch to the exact reviewed draft, reload/reopen the saved packet and verify its original bytes and anchor. Capture evidence and update the draft PR.

```sh
harness dsh install https://github.com/autonomous-ai/openharness --ref codex/typst-user-playtest --path store/viewers/doc-viewer
```

Typst itself remains the official catalog package at `22b15928`. Install log: `/private/tmp/openharness-typst-review-install.log`. Earlier package recovery copies: `/private/tmp/openharness-rdkit-typst-recovery`. Browser screenshot remains in the test session and was not uploaded. The user requested a pause before the native output-use lease; native review/revision/reopen is not complete.
