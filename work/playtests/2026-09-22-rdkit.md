# RDKit playtest handoff — 2026-09-22

Branch: `codex/rdkit-user-playtest`. [Draft PR 230](https://github.com/autonomous-ai/openharness/pull/230). No merge. Code fix: `4770d670`; subsequent changes only document the handoff.

## Fixed and verified

Molecule names containing `#`, `?` or `%` broke model, ensemble and thumbnail URLs; Unicode download names could fail while writing the HTTP header. The pane now encodes each filename component and sends safe, UTF-8-aware attachment names.

- In real Chrome with pinned native RDKit output, the old pane displayed “Waiting for … 404” and broken thumbnails. The fixed pane opened `candidate #1`, `candidate ?2`, `candidate %3` and `α lead #4`, including the latter's five-conformer ensemble.
- Clicked the Unicode SDF export; download SHA-256 matched the source: `b805b416f334b96685589e8589377e34edd8b8d91702dfbe12182212fbe68a13`.
- Used the actual bond-scan controls for a 25-pose O3–C4–C5–C6 rigid MMFF94 scan, selected −165° (ΔE 0.29), kept the named study and note, then reloaded and reopened it. The standalone reproducer reported zero energy and coordinate difference.
- `npm test` in `store/agents/rdkit`: 29/29 pass. `harness dsh check store/agents/rdkit`, JavaScript syntax and `git diff --check` pass. All 138 Python baseline checks were covered across the main run and two explicit native-runtime reruns. GitHub `models-and-viewer` passed for the code change.

The browser journey used a separately generated scratch workspace, not an embedded-agent prompt.

## Actual Harness prompt completed; native output interaction pending

Opened Store → RDKit → Try this prompt → Start Harness with the installed review branch. The actual embedded Codex built butane and phenethyl acetate and visibly updated the model. Submitted a follow-up requesting a butyl/methyl/ethyl paraben series with explicit hydrogens and computed-only properties.

Continue in tab **Build conformers and compare**, project `~/harnesses/rdkit-2026-09-22-13-10`. The generated names are `candidate #1`, `candidate ?2` and `lead #4`. Greek alpha did not survive the native input/agent result; Unicode evidence belongs to the browser check above.

Read-only native chemistry checks confirm formulas C11H14O3 / C8H8O3 / C9H10O3, explicit hydrogens and finite coordinates across 23 / 5 / 5 samples. The agent explicitly labels the small analogues' extra samples as potentially symmetry-equivalent, not five unique minima. All 18 pre-series artifact/script hashes remain unchanged.

## Resume

1. Select the prompted paraben series in native Harness; check its model, thumbnails and conformer controls.
2. Scan a flexible lead bond, select and keep a pose with a note, reload and reopen the study. Then ask the embedded agent to continue from that exact saved study while preserving it.
3. Reserve preview/desktop/browser interaction for the tester in any follow-up prompt; the embedded agent should author files and inspect artifacts. Other agents' desktop control caused focus interference during this session.
4. Capture and publish final evidence; keep the native interaction/revision boundary explicit until completed.

Installed package is a self-contained official-repository clone at `4770d670`, not a linked worktree. Reinstall command:

```sh
harness dsh install https://github.com/autonomous-ai/openharness --ref codex/rdkit-user-playtest --path store/agents/rdkit
```

Local evidence: `/private/tmp/openharness-rdkit-user-playtest/.scratch/filename-regression/workspace`. Kept study: `out/torsions/485a194c-1527-4571-a5cf-f8c6b983124d/`, including `study.zip`, `selected.sdf`, `reproduce.py` and `reproduced.json`. Detailed chronology: `.scratch/playtest/journeys.md`. Recovery packages: `/private/tmp/openharness-rdkit-typst-recovery`. Old Chrome screenshots remain in the test session and were not uploaded to the PR. Pause requested by the user before the next native lease; no output-use pass is implied.
