# Partition table for the 16MB flash on the ESP32-S3-Touch-AMOLED-1.75C.
# Dual-OTA: two app slots + otadata, so firmware can be updated over the air.
# nvs stays at the SAME offset/size as the old single-app layout so the pairing
# token + saved-WiFi list survive the one-time USB flash to this table (no re-pair).
# Name,   Type, SubType, Offset,   Size
nvs,      data, nvs,     0x9000,   0x6000
otadata,  data, ota,     0xf000,   0x2000
phy_init, data, phy,     0x11000,  0x1000
ota_0,    app,  ota_0,   0x20000,  0x7E0000
ota_1,    app,  ota_1,   0x800000, 0x7E0000
