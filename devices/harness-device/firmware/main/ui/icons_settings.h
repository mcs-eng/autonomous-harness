#pragma once
#include "lvgl.h"

extern const lv_image_dsc_t icon_voice;
extern const lv_image_dsc_t icon_wifi;
extern const lv_image_dsc_t icon_newproj;
extern const lv_image_dsc_t icon_battery;
extern const lv_image_dsc_t icon_brightness;   // Figma sun (blue disk + white rays), transparent bg
extern const lv_image_dsc_t icon_lock;         // white pattern-lock — screen-lock passcode row
extern const lv_image_dsc_t icon_loop;         // Overview "Loop" repeat arrows (Figma)
extern const lv_image_dsc_t icon_reset;        // Settings "Reset device" restart-line, red baked in
extern const lv_image_dsc_t icon_info;         // Settings "Version" information-fill (white disc, cut-out i)
extern const lv_image_dsc_t icon_act_voice;    // agent tile action: mic, 44px, #00ff2f baked
extern const lv_image_dsc_t icon_act_loop;     // agent tile action: repeat arrows, 44px, #5b8cff baked
