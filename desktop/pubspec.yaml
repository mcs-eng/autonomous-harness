name: harness
description: "Harness desktop terminal client."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  lucide_icons_flutter: ^3.1.15
  dio: ^5.11.0
  web_socket_channel: ^3.0.3
  flutter_riverpod: ^2.6.1
  go_router: ^17.5.0
  url_launcher: ^6.3.2
  cryptography: ^2.9.0
  # Unicode normalisation for the viewer's password-linking PAKE: the same
  # password must hash identically whatever form the keyboard produced it in.
  # Pure Dart, so it registers no native plugin and leaves the macOS SPM
  # package list untouched — see CLAUDE.md on what a plugin costs here.
  unorm_dart: ^0.3.2
  # Keep the terminal core in-tree until the upstream circular-buffer ownership
  # fix is released. See third_party/xterm/lib/src/core/buffer/buffer.dart.
  xterm:
    path: third_party/xterm
  package_info_plus: ^10.2.1
  window_manager: ^0.5.2
  # The system folder panel: NSOpenPanel on macOS, IFileDialog on Windows, the
  # XDG portal on Linux. Used ONLY for a machine that is this computer — see
  # `widgets/new_agent_dialog.dart`.
  file_selector: ^1.0.3
  # Reads OpenCode's own database for the usage ledger — `usage/ledger/`. The
  # Dart-only FFI package on purpose, NOT `sqlite3_flutter_libs`: this dlopens
  # the sqlite the OS already ships (every macOS has one, every Ubuntu desktop
  # has one), so it registers no native plugin and leaves the macOS SPM package
  # list untouched — see CLAUDE.md on what a plugin costs here. A machine
  # without the library reports `OpenCodeUnavailable` rather than failing to
  # build, which is the same shape the other two scanners use for "nothing to
  # read".
  sqlite3: ^2.4.0
  desktop_drop: ^0.8.4
  path: ^1.9.1
  # The web pane: a domain harness's viewer (Circuit's board, Workshop's part)
  # drawn beside its agent's terminal. THE one native plugin this feature adds
  # — WKWebView via the endorsed `webview_flutter_wkwebview`, which lands in
  # the macOS SPM package list and GeneratedPluginRegistrant. macOS only for
  # now; the pane shows the URL as text where no implementation is registered
  # (Linux, and every `flutter test`). See CLAUDE.md on what a plugin costs.
  webview_flutter: ^4.14.1
  # Named directly for the WebKit creation params (muted autoplay in viewer panes).
  webview_flutter_platform_interface: ^2.15.1
  webview_flutter_wkwebview: ^3.26.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Only for the test that stands in for the OS folder panel, which a widget
  # test cannot open — see `test/new_agent_codex_profile_test.dart`.
  file_selector_platform_interface: ^2.7.0
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/engine-icons/
    - assets/store/
    # The real app icon, shown in Settings ▸ About. Copied from the macOS
    # icon set (`macos/Runner/Assets.xcassets/AppIcon.appiconset`) rather than
    # loaded from the bundle: Flutter can't read an .icns, and About should show
    # the mark the Dock shows, not a stand-in drawn in Dart.
    - assets/app_icon.png
    - assets/harness_device.png
    - assets/harness_device_studio.png
    - assets/harness_store_card.png
    - assets/swarm-wallpapers/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
