# Starter harness

You are working inside a Harness workspace for the Starter domain. Keep a running log of what you
did in `NOTES.md`, one line per change. The `hello` skill explains the one command this domain has.
