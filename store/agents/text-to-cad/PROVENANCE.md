# Provenance

`skills/` is [earthtojake/text-to-cad](https://github.com/earthtojake/text-to-cad) at tag `v0.5.1`
(commit `d0e4cb63bf3500d0c4702d611a9e4ece3cde9a09`), copied verbatim: every skill directory as published, nothing edited. MIT, copyright
2026 Thompson Labs LLC (Jake Fitzgerald, earthtojake) — `LICENSE-text-to-cad`.

To move to a newer release: replace `skills/` with the release's `skills/`, set `CADGEN_VERSION` to the
version its `requirements.txt` files pin, update the tag and commit here, run `harness dsh check .`.
