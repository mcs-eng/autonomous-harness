# Strudel user playtest — 2026-09-22

Draft PR: [#231](https://github.com/autonomous-ai/openharness/pull/231). No merge.
Branch: `codex/strudel-take-loops`.

## Completed

- A real prompt in a fresh native Harness project produced **Harbor Lights**, a 96 bpm five-voice
  synth track. The old installed package lacked the recorder because the Store did not offer
  an update after the official repository rename; the separate Store fix is PR223.
- Used that actual track in the patched viewer in Chrome: Play, record, mute/solo, mark two
  passages, finish with Space, name, keep, reload and reopen. The actual stereo take is 32.832 s,
  1,575,936 frames at 48 kHz, with its source and timed mix events.
- Verified marked passage loops of 10.496 s and 10.954667 s, waveform highlighting, return to the
  matching full-take time while paused, live/recorded playback exclusion and keyboard mixer state.
  An end-marker regression was fixed and retested using an explicitly labeled copy of the take.
- Original full WAV and source hashes stayed unchanged. The WAV peak is 1.480566 (+3.4 dBFS),
  including 9,736 samples above full scale. The added take-level warning reports measured overload
  without normalizing or modifying audio. Reopened the real saved take in Chrome and verified
  the visible +3.4 dBFS warning and its layout.
- All 27 Node and 51 Python tests, package conformance, doctor and syntax checks passed.
- Installed this branch and submitted a new original prompt through Harness. The native project
  `strudel-2026-09-22-13-22` has a new 40-line offline five-voice track, call/answer melody,
  conservative gains and a clean parsing verdict.

## Resume here

The fresh native project's performance interaction and revision are unfinished. Play and record,
use Mute/Solo, mark a passage, loop it before saving, name and keep it, reload and reopen.
Then ask the embedded agent to develop the marked phrase from the kept journal/source while
preserving the saved take. The test operator owns preview controls; the agent should edit files
and inspect artifacts without controlling the desktop or browser. Measure the new WAV's actual
headroom; low gains in source alone do not prove it stays below full scale.

The completed Chrome take is `1a325927-0b8c-4d63-baad-a853c6c093fd` in the local
`strudel-app-review-9oe1fv6l` workspace. Its WAV SHA256 is
`c75ccab37e61302584fe9e5b2b560b3fc156b55dbd03d25aac125a595e970b1c`.
Scratch native baselines live in this branch's `.scratch/native-rerun-baseline/`; these are
local resume aids, not committed fixtures. The PR includes an approved screenshot of the real take.

Parsing and signal measurements do not establish musical quality. No subjective listening
approval or full native acceptance pass is claimed.
