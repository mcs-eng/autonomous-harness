# Jev Sheets user playtest — 2026-09-22

Draft PR: [#218](https://github.com/autonomous-ai/openharness/pull/218). No merge.
Branch: `codex/jev-sheets-kept-questions`.

## Completed

- Created a fresh project through the native Harness Store and submitted an original twelve-ticket
  offline workshop prompt to its embedded coding agent. The baseline exposed an unwanted live
  provider call, HTTP 402 and misleading completion state. These are fixed on this branch.
- Used the actual generated sheet in the patched viewer in Chrome: compare urgency wording,
  mark a row preference, write a note, keep the trial, adopt the wording, restart the viewer and
  reopen. Both questions, the note, offline mode and **Saved in project** persisted.
- Installed this branch and created another native project, `jev-sheets-2026-09-22-13-20`
  (tab **Hosting support tickets practice sheet**). Its original prompt produced twelve fictional
  tickets, `offline:true`, two questions, 24/24 filled cells and `client:mock`. The exercise stayed
  in the pane; no script or chat substitute was authored.
- All 70 Node tests, package conformance, doctor and syntax checks passed. Tests cover persistent
  adoption, stale input, idempotence, offline/live transitions and a loopback HTTP 402 provider.

## Resume here

The native rerun's interactive Question Lab review, adoption, follow-up agent revision and
reopen are unfinished. Use the existing project above. Compare
`Production is down or customers cannot complete checkout?`, add a preference and review note,
keep and adopt it, then ask the agent to continue from the saved review without changing the
original data or question. Reopen and verify both questions and the review packet survive.
The test operator should own preview controls; tell the embedded agent to edit files and inspect
artifacts without controlling the desktop or browser.

The completed Chrome packet is `6987de23-0862-45c4-b187-c8b1daa1b241` in the local
`jev-app-practice-jkuvp4w5` workspace. Scratch baselines and exact hashes live in this branch's
`.scratch/native-rerun-baseline/`. These are local resume aids, not committed fixtures.

The configured live provider was out of credit. Offline results verify interaction and persistence,
not Jev answer quality. Do not describe these checks as a complete native acceptance pass.
