{
  "name": "autonomous-phaser",
  "private": true,
  "description": "Phaser as a Harness agent: one node_modules for every workspace, a Vite dev server as the pane",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "python3 -m unittest discover -s toolchain && node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "terser": "^5.39.0",
    "vite": "^6.3.1"
  }
}
