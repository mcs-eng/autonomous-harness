#!/usr/bin/env bash
# init-workspace — runs after the template copy, cwd = the workspace. Nothing more needed for Jev Arena
# (arena.json is already in the template); kept for the standard shape so the harness can seed future
# worlds without an app change.
set -euo pipefail
exit 0
