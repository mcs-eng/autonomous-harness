# Complete the researched Harness shortlist

Branch: `codex/remaining-harnesses`
Worktree: `/private/tmp/harness-remaining-studios`

The September 16 shortlist has eight remaining packages. Preserve upstream names and credit:
JUCE Agent Toolkit, Foam-Agent, autoresearch-mlx, Ableton AI, DimOS, SimSkill, Bonsai MCP,
and official Comfy MCP. Godogen and OpenMontage already exist.

## Delivery

- Pin and inspect each upstream; package its instructions and executable integration.
- Give each workspace a useful starter and a domain-specific interactive pane: an instrument
  workbench, flow field, experiment ledger, clip sequencer, mission map, traffic playground,
  building inspector, and generation gallery.
- Reuse one small viewer host for filesystem containment, live updates, job state, history,
  and exports. Keep domain behavior with its agent package.
- Preserve the last valid result on a failed run, show progress and actionable failures,
  support keyboard interaction, reduced motion, and narrow panes.
- Exercise installation, materialization, viewer launch, domain controls, execution, artifacts,
  reload, failure/recovery, and export. Measure code coverage and record exact denominators.
- Commit and push each working milestone. Do not publish the main Store catalog from this branch.

## Verification boundary

The user confirmed: **use this Mac and local simulations**. The end-to-end acceptance matrix
covers the shipped local workflows on this machine; native service/hardware connections are
separate optional paths and must not be presented as tested here.

This Mac initially has no Ableton Live, Docker/OpenFOAM, Blender/Bonsai, or robot hardware.
Local examples must identify their actual engine and provenance. A procedural preview,
simulation, fixture, or protocol-contract test cannot count as live upstream verification.
Native dependencies and external services get explicit doctor checks and separate integration
results. No claim of 100% end-to-end coverage is justified while an advertised path is untested.

## Progress

- [x] Inventory current packages and create isolated branch/worktree.
- [x] Refresh upstream source pins and installation contracts.
- [x] Build and test the common viewer host.
- [x] Build and test all eight domain packages.
- [x] Exercise packages through the Harness lifecycle.
- [x] Capture real viewer screenshots and update research status.
- [x] Record coverage and outstanding external checks; push final checkpoint.

## Delivered

All eight remaining packages have pinned upstream sources, local artifact workflows, native
adapters where applicable, skills, and interactive Studio Viewer panes. The user-selected Mac
simulation scope is complete. See [the verification report](../../store/viewers/studio-viewer/TESTING.md)
for the exact 100% coverage denominators, Chrome/WebKit matrix, real Harness lifecycle checks,
and external applications that were represented by contract fixtures. The main Store catalog
was not published. Work is committed and pushed on the branch above.
